// Slots router — /api/slots (LLD §2.4)
//
// Task 1.3: GET /?slug=&date=YYYY-MM-DD
//           Loads event type + availability rule + confirmed bookings,
//           delegates to services/slots.ts, returns { slots: Slot[] }
