# Frontend

Next.js 14 app for the Cal.com clone. See the **[root README](../README.md)** for setup, architecture, and deployment.

```bash
# From repo root
make run          # starts backend + frontend
make frontend     # UI only → http://localhost:3000
```

Environment: copy `.env.example` to `.env.local`.
